#include <iostream>

int main()
{
    int n;
    std::cin >> n;

    for (int i = 0; i < n; ++i)
    {
        std::cout << '*';
        for (int j = 0; j < 1; j++)
        {
            std::cout << '@';
        }
    }

    return 0;
}
